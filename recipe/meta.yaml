{% set name = "pyquery" %}
{% set version = "1.3.0" %}
{% set hash_type = "sha256" %}
{% set hash_value = "5821ffd02301db701cb30f2483ab96774c2b6b70530f79a44645f49dc5053ab8" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  {{ hash_type }}: {{ hash_value }}

build:
  number: 0
  script: python setup.py install  --single-version-externally-managed --record=record.txt

requirements:
  host:
    - python
    - setuptools
  run:
    - python
    - lxml >=2.1
    - cssselect >0.7.9

test:
  imports:
    - pyquery

about:
  home: https://github.com/gawel/pyquery
  license: BSD 3-Clause
  license_family: BSD
  license_file: LICENSE.txt
  summary: A jquery-like library for python
  description: |
      pyquery allows you to make jquery queries on xml documents. The API is as
      much as possible the similar to jquery. pyquery uses lxml for fast xml
      and html manipulation.
  doc_url: http://pyquery.rtfd.org
  dev_url: https://github.com/gawel/pyquery

extra:
  recipe-maintainers:
    - nehaljwani
